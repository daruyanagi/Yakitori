<Application x:Class="Yakitori.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Yakitori"
             >
    <Application.Resources>

    </Application.Resources>

    <JumpList.JumpList>
        <JumpList>
            <JumpTask Title="Active Window (Clipboard)" 
                      Arguments="/a" />
            <JumpTask Title="Desktop (Clipboard)" 
                      Arguments="/d"/>
            <JumpTask Title="Desktop (File)" 
                      Arguments="/f" />
            <JumpTask Title="Regeon (Clipboard)" 
                      Arguments="/r" />
            <JumpTask Title="Save Clipboard Image to Folder" 
                      Arguments="/s" />
            <JumpTask Title="Open Folder" 
                      Arguments="/o" />
            <JumpTask Title="Settings" />
        </JumpList>
    </JumpList.JumpList>
</Application>
